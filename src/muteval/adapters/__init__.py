"""Adapters that let muteval grade eval suites written for other tools.

These are optional and import their third-party dependency lazily, so the
muteval core stays dependency-free. Install the matching extra to use one:

    pip install "muteval[deepeval]"
"""
